import { NextFunction } from "express";
import { IReq, IRes } from "../../../utils/reqResInterfaces";

export default async function uploadNewImgsIfPresent(req: IReq, res: IRes, next: NextFunction) {
  // TODO: implement this
  next();
}